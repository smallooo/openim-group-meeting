# 商品消息测试指南

## 🔍 问题诊断

如果聊天页面显示"暂不支持的消息类型"，请按以下步骤进行诊断：

### 1. 检查消息发送日志

在商品详情页面点击客服按钮后，查看控制台输出：

```
🔍 构建的商品消息数据: {"customType":920,"data":{...}}
🔍 消息类型: 920
🔍 创建的消息对象: {...}
```

### 2. 检查消息接收日志

在聊天页面查看控制台输出：

```
_buildCustomTypeItemView - 消息类型: 100
_buildCustomTypeItemView - 消息内容: {"customType":920,"data":{...}}
_buildCustomTypeItemView - 解析结果: {...}
_buildCustomTypeItemView - viewType: 920
```

### 3. 常见问题排查

#### 问题1：消息类型不匹配
- **现象**：viewType 不是 920
- **原因**：消息解析失败
- **解决**：检查消息数据格式是否正确

#### 问题2：data['data'] 为 null
- **现象**：data['data'] 显示为 null
- **原因**：消息数据结构不正确
- **解决**：检查消息构建逻辑

#### 问题3：消息类型显示为其他值
- **现象**：viewType 显示为其他数字
- **原因**：CustomMessageType 常量值不正确
- **解决**：检查 CustomMessageType.productShare 的值

## 🧪 手动测试步骤

### 步骤1：发送测试消息
```dart
// 在商品详情页面调用
logic.testSendProductMessage();
```

### 步骤2：检查消息数据
确认消息数据包含：
- `customType: 920`
- `data` 对象包含商品信息

### 步骤3：验证消息解析
确认解析结果包含：
- `viewType: 920`
- `data` 对象不为 null

## 🔧 修复方案

如果问题仍然存在，请检查：

1. **消息类型常量**：
   ```dart
   // 确认 CustomMessageType.productShare = 920
   print('CustomMessageType.productShare: ${CustomMessageType.productShare}');
   ```

2. **消息数据格式**：
   ```dart
   // 确认消息数据格式正确
   final expectedData = {
     "customType": 920,
     "data": {
       "productId": "...",
       "productName": "...",
       // ... 其他字段
     }
   };
   ```

3. **解析逻辑**：
   ```dart
   // 确认解析逻辑正确
   case CustomMessageType.productShare:
     map['data']['viewType'] = CustomMessageType.productShare;
     return map['data'];
   ```

## 📞 如果问题持续存在

请提供以下信息：
1. 控制台的完整日志输出
2. 消息发送时的数据格式
3. 消息接收时的解析结果
4. 具体的错误信息

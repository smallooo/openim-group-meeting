# 商品自定义消息功能说明

## 📋 功能概述

本功能实现了在IM聊天中发送商品自定义消息，当用户在商品详情页面点击客服按钮时，会将商品信息以卡片形式发送给指定的客服。

## 🚀 实现的功能

### 1. 自定义消息类型
- **商品分享消息** (`CustomMessageType.productShare = 920`)
- **商品咨询消息** (`CustomMessageType.productInquiry = 921`)

### 2. 商品信息包含
- 商品ID、名称、副标题
- 商品图片
- 价格信息（支持SKU价格）
- 品牌、分类信息
- 店铺信息
- 卖家信息
- 描述信息

### 3. 消息显示
- 在聊天页面以卡片形式显示商品信息
- 支持商品图片展示
- 美观的UI设计，包含价格、品牌等关键信息

## 📁 涉及的文件

### 核心文件
1. **`openim_common/lib/src/extension/message_manager.dart`**
   - 添加了商品相关的自定义消息类型

2. **`openim_common/lib/src/utils/utils.dart`**
   - 添加了商品消息的解析支持

3. **`lib/tk_app/pages/tk_channel/tk_product_detail/logic.dart`**
   - 实现了发送商品消息到客服的核心逻辑

4. **`lib/pages/chat/chat_view.dart`**
   - 实现了商品卡片的显示组件

## 🔧 使用方法

### 1. 发送商品消息
在商品详情页面，点击右上角的客服按钮（耳机图标），系统会：
1. 获取当前商品信息
2. 获取选中的SKU信息
3. 构建商品消息数据
4. 发送给指定的客服
5. 跳转到聊天页面

### 2. 配置客服ID
在 `logic.dart` 的 `_getCustomerServiceID()` 方法中配置客服的IM ID：

```dart
String? _getCustomerServiceID() {
  // 这里可以从配置、API或固定值获取客服ID
  return 'customer_service_001'; // 替换为实际的客服ID
}
```

### 3. 自定义消息数据
商品消息的数据结构：

```json
{
  "customType": 920,
  "data": {
    "productId": "商品ID",
    "productName": "商品名称",
    "productSubName": "商品副标题",
    "productImage": "商品图片URL",
    "price": 价格,
    "skuName": "SKU名称",
    "brandName": "品牌名称",
    "categoryName": "分类名称",
    "sellerName": "卖家名称",
    "shopName": "店铺名称",
    "description": "描述信息",
    "timestamp": 时间戳
  }
}
```

## 🎨 UI展示

### 商品分享卡片
- 显示商品图片
- 商品名称和副标题
- 价格信息（红色突出显示）
- 品牌标签
- 店铺信息
- 描述信息

### 商品咨询卡片
- 蓝色主题
- 购物袋图标
- 简洁的商品信息展示

## 🧪 测试功能

在 `logic.dart` 中提供了测试方法 `testSendProductMessage()`，可以用于调试和验证功能：

```dart
// 在商品详情页面调用测试方法
logic.testSendProductMessage();
```

## 🔄 后续扩展

### 1. 后台发送消息
可以通过OpenIM的REST API实现后台发送商品消息：

```dart
// 后台发送商品消息示例
await BackendMessageService.sendProductShareToUser(
  userID: 'customer_service_001',
  productId: '123',
  productName: '商品名称',
  productImage: 'https://example.com/image.jpg',
  price: 99.99,
);
```

### 2. 消息交互
可以添加点击商品卡片跳转到商品详情页面的功能：

```dart
// 在商品卡片上添加点击事件
GestureDetector(
  onTap: () {
    // 跳转到商品详情页面
    AppNavigator.startProductDetail(productId: data['productId']);
  },
  child: _buildProductShareCard(data),
)
```

### 3. 更多商品信息
可以根据需要添加更多商品信息，如：
- 库存信息
- 促销信息
- 评价信息
- 相关商品推荐

## ⚠️ 注意事项

1. **客服ID配置**：确保正确配置客服的IM ID
2. **网络图片**：商品图片需要是可访问的URL
3. **消息大小**：注意自定义消息的数据大小限制
4. **错误处理**：已添加基本的错误处理和用户提示

## 📞 技术支持

如有问题，请检查：
1. OpenIM SDK是否正确初始化
2. 客服ID是否正确配置
3. 网络连接是否正常
4. 商品数据是否完整
